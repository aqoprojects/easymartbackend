version: '3.9'

services:
  web:
    build: .
    command: uvicorn easymart.asgi:application --host 0.0.0.0 --port 8000 --workers 3
    env_file: .env
    expose:
      - "8000"

networks:
  default:
    name: easymart_network
